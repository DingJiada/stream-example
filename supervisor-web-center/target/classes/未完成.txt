本项目未完成内容：
1.permId未正规化。permId为某个菜单的id。测试期间当时菜单的整体功能未开发完成，无法获得permId，所以让前端一律传的数字0
2.后台每个非匿名接口未判断权限，原因跟permId一样，记得补上。
3.操作记录日志整个功能未完成。
  |- 3.1 向前端返回操作记录日志详情(LogOperDetail)时如何过滤掉共用字段，如：是否删除、删除时间
  |      是从插入时解决？还是在向前端返回时解决？
  |- 3.2 在插入操作记录日志详情(LogOperDetail)时解决掉JSON.toJSONString(o)序列化后的双引号问题，会导致前端‘去转义’时失败